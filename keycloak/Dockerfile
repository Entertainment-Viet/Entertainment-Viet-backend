ARG KEYCLOAK_CONFIG_CLI_VERSION=5.2.0
ARG KEYCLOAK_VERSION=20.0.1

FROM adorsys/keycloak-config-cli:${KEYCLOAK_CONFIG_CLI_VERSION}-${KEYCLOAK_VERSION}

COPY import /

# Configurable Environment Variables
ENV ENVIRONMENT=local