spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}
  jpa:
    hibernate:
      ddl-auto: validate
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://18.143.189.238:8443/auth/realms/ve-sso

security:
  allowed_origins:
    - http://host.docker.internal:3000
    - http://localhost:3000

authentication:
  keycloak:
    serverUrl: https://18.143.189.238:8443/auth